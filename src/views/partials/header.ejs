<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'Beauty Academy' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
<div class="noise"></div>
<header class="nav glass">
  <a href="/" class="logo">Beauty<span>Scale</span></a>
  <nav>
    <a href="/">Каталог</a>
    <% if (!currentUser) { %>
      <a href="/login">Вход</a>
      <a href="/register" class="btn small">Регистрация</a>
    <% } else { %>
      <a href="/dashboard">Кабинет</a>
      <form method="post" action="/logout" class="inline"><button class="btn small ghost">Выйти</button></form>
    <% } %>
  </nav>
</header>
<main>
